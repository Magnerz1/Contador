const load = () =>{

};

document.load = load;

/** Guarda valores en el local storage */
const set = (key, value) => {
    localStorage.setItem(key. value)
};
const get =() => {
    if (key) {
        throw new Error ("")
    }
};
// adadadad
// adad
// asdasdadas
// adadaaLL